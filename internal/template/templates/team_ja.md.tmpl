# {{.ProjectName}}

{{if .Description}}{{.Description}}{{else}}内部プロジェクトの説明。{{end}}

## 概要

このリポジトリには...

## 構造

{{.Structure}}

## はじめに

### 前提条件

- Go 1.23以上（Goプロジェクトの場合）
- Node.js 20以上（TypeScriptプロジェクトの場合）

### セットアップ

{{if eq .Language "go"}}```bash
git clone <repository-url>
cd {{.ProjectName}}
go mod download
```{{else if eq .Language "typescript"}}```bash
git clone <repository-url>
cd {{.ProjectName}}
bun install
```{{else}}```bash
git clone <repository-url>
cd {{.ProjectName}}
# セットアップ手順をここに記載
```{{end}}

### 開発

```bash
# ローカル実行
{{if eq .Language "go"}}go run ./cmd/{{.ProjectName}}{{else}}bun run dev{{end}}

# テスト実行
{{if eq .Language "go"}}go test ./...{{else}}bun test{{end}}
```

## 設定

| 変数 | 説明 | デフォルト |
|------|------|-----------|
| `EXAMPLE_VAR` | 説明 | `default` |

## デプロイ

デプロイ手順をここに記載。

## お問い合わせ

質問があればチームに連絡してください。
